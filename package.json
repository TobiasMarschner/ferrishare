{
  "name": "e2ee-fileshare-rust",
  "version": "0.1.0",
  "description": "This project uses both `cargo` and `npm` to manage packages.",
  "main": "index.js",
  "scripts": {
    "dev:tw": "npx tailwindcss -i ./main.css -o ./static/main.css --minify --watch",
    "build:tw": "npx tailwindcss -i ./main.css -o ./static/main.css --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.14"
  }
}
